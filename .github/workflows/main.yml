name: My-Action-Github

on:
  push:
    branches: [ master ]

jobs:
  my-testing:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2

    - name: Run-linter
      run: make lint
      
      
