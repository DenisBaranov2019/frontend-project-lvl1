#----------------------
name: My-Action-Github

on:
  push:
    branches:
      - master

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: install
        run: |
          make install
        
      - name: lint
        run: make lint
